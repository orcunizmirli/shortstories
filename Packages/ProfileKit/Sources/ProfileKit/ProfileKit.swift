import AnalyticsKit
import AppFoundation
import DesignSystem

/// ProfileKit — Profil, Ayarlar, hesap bağlama/auth UI (KANON §4).
/// F1 kapsamı: SS-130…SS-133 (docs/09 §E12; hesap bağlama Apple F1, Google/e-posta F2)
/// + altyazı/uygulama dili ayrımı SS-161 (§E15).
/// ContentKit'e BAĞIMLI DEĞİLDİR (R3).
public enum ProfileKitModule {
    public static let name = "ProfileKit"
}
